# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/

# Secrets
.env

# Persistent storage & logs
data/_storage/
data/_cache/
logs/

# Jupyter
.ipynb_checkpoints/
*.ipynb_*.bak

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
shioaji.log
.coverage
certs/

# v0.1.16 v2 deployment staging (transient)
_v0_1_16_staging/

# v0.1.16 v2 deployment backup files (transient, kept for emergency rollback)
*.pre_v0_1_16_v2.bak

# v0.1.16 v2.1 hotfix backup files (transient, kept for emergency rollback)
*.pre_v2_1*.bak
*.pre_swap.bak

# Runtime lock files (cron jobs use file-locks to prevent concurrent runs)
.lock/

# Catch-all for transient .pre_* backup files (any patch session)
*.pre_*.bak

# DuckDB and SQLite database files
*.db
*.duckdb

# Local scratch (commit message files, dry-run outputs, transient
# governance artifacts). Files under tmp/ are NOT part of the
# repository audit trail; they are local working copies. Permanent
# audit artifacts go to research/r8_phaseN/ instead.
tmp/

