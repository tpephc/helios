# Helios v0.1.16 Session Handoff — 2026-05-27

**Status:** v0.1.16 MERGED → main (019d46f)
**Next:** v0.1.17 — execution_submitter + schema migration

---

## 1. Git HEAD

```
019d46f (HEAD -> main) merge: v0.1.16 complete
d21c17d docs: handoff §8 — cron observation, merge approved
35065b7 docs: backlog #25 — intraday_monitor recovery notification
9cc5add fix: ShioajiQuoteSource — simulation=True per settings
64dcf20 research: mae_atr_study.py — exploratory MAE/ATR study
7e9d76d docs: backlog #18 — Advisor E geometric return reference
960d245 docs: backlog #24 — MACD + Lower-Highs-Lows
524e3cd fix: find_bearish_stocks — 3 logic fixes (Advisor D)
94d30c8 fix: format_entry_request — stock short_name
```

---

## 2. 今天完成的工作

### 2.1 Cron timing 問題發現與修正

**問題：** `shioaji_download_daily.py` 在 14:50 取得 1089 rows（partial）
被 `_MIN_EXPECTED_ROWS=1500` guard 擋住，pipeline 中止。

**根本原因：** Shioaji `daily_quotes` 在 14:50 資料尚未完整發布：
- 14:50 TST：1089 rows（partial）
- 17:33 TST：1976 rows（完整）

**修正：** Cron 從 14:50 改回 16:00。`signal_preview` 從 14:55 改到 16:05。

**待觀察：** 明天需要確認 16:00 時 daily_quotes 是否穩定有 1500+ rows。
如果 16:00 仍然 partial，需要進一步往後調整或改用 snapshots()。

**當前 cron 時序：**
```
16:00  shioaji_download + build_adjusted + compute_features +
       bearish/bullish features + daily_run
16:05  signal_preview
16:30  find_bearish_stocks
16:35  score_short_candidates
16:40  find_bullish_setups
15:30  auto_backup
19:30  evening_digest
09-13  intraday_monitor (每 15 分鐘)
```

### 2.2 ShioajiQuoteSource simulation fix

**問題：** `monitoring/quote_source.py` 用 `sj.Shioaji()`（production mode），
simulation token 沒有 production 權限 → `intraday_monitor` 報價失敗。

**修正：** 改為 `sj.Shioaji(simulation=cfg.shioaji_simulation)`

**驗證：** 13:05–13:50 全部 `positions_checked=2, symbols_succeeded=2` ✅

### 2.3 手動建立測試部位

手動插入兩個部位供 `intraday_monitor` 測試：

| position_id | symbol | entry_price | shares | status |
|---|---|---|---|---|
| pos_556e7203279f | 4919 新唐 | 209.0 | 1000 | OPEN |
| pos_b6154f95169d | 2891 中信金 | 58.05 | 1000 | OPEN |

兩個部位均為 `is_synthetic=True`。
停損水位：4919=179.48（2×ATR）、2891=55.25（2×ATR）。

### 2.4 MAE/ATR exploratory study

**腳本：** `research/mae_atr_study.py`

**結果（2022-2026，2013 signals）：**

| 指標 | 值 |
|---|---|
| P50 MAE | 1.84×ATR |
| 2×ATR survival | 52.7% |
| 3×ATR E[P&L] | +1.68×ATR（最高） |
| 2×ATR E[P&L] | +1.40×ATR |

**初步結論：** 2×ATR 是中位數停損點（每次進場中位數回撤≈停損線），
3×ATR 期望值更高。但這是 draft 結果，需修正 entry price 語意後再確認。

**P0 fixes required before [CALIBRATED]：**
- entry_price 改用 next_day adj_open（目前用 signal day close）
- MFE 改用 adj_high（目前用 adj_close，與 MAE adj_low 不對稱）
- Batch SQL queries（目前 N+1 per signal）
- Output metadata manifest

### 2.5 find_bearish_stocks 3 logic fixes (Advisor D)

| Fix | 改動 |
|---|---|
| partial_align | 加 `d20 < 0` 要求（排除短線回檔）|
| RSI scoring | RSI 30-45 = +25（走弱），RSI<30 = +12（超跌降分）|
| --all bug | 補傳 `include_none=args.all` |

### 2.6 format_entry_request stock name

標題從 `4919 (unknown)` 改為 `新唐 4919`。
`_get_display_name()` 查 `company_metadata.short_name`。

### 2.7 Backlog 新增

| # | 標題 |
|---|---|
| #24 | MACD + Lower-Highs-Lows feature expansion |
| #25 | intraday_monitor recovery notification |

---

## 3. 完整 Backlog

| # | 標題 | Priority | Version |
|---|---|---|---|
| #15 | Decouple signal/submission | P0 | v0.1.17 |
| #16 | Open-gap calibration INV-2 | P1 | v0.1.17 |
| #17 | READY_FOR_SUBMISSION schema | P0 | v0.1.17 |
| #18 | Bearish outcome study | P0 | v0.2 |
| #19 | bullish_features layer | — | v0.2 |
| #20 | Notification abstraction | — | v0.2 |
| #21 | Shioaji daily_quotes pipeline | — | v0.2 |
| #22 | daily_price source provenance | — | v0.2 |
| #23 | Multi-account architecture | P0 | v0.1.17-B, v0.1.18 |
| #24 | MACD + Lower-Highs-Lows | — | v0.2 |
| #25 | intraday_monitor recovery notification | — | v0.2 |

---

## 4. DB 狀態

```bash
uv run python -c "
import duckdb
conn = duckdb.connect('data/_storage/helios.duckdb', read_only=True)
print('positions OPEN:')
for r in conn.execute(\"SELECT symbol, entry_price, shares, status FROM positions WHERE status='OPEN'\").fetchall(): print(' ', r)
print('daily_price max:', conn.execute('SELECT MAX(date) FROM daily_price').fetchone())
print('daily_features:', conn.execute('SELECT COUNT(*), MAX(date) FROM daily_features').fetchone())
print('bearish_features:', conn.execute('SELECT COUNT(DISTINCT stock_id), MAX(date) FROM bearish_features').fetchone())
print('bullish_features:', conn.execute('SELECT COUNT(DISTINCT stock_id), MAX(date) FROM bullish_features').fetchone())
"
```

Expected:
- positions OPEN: 4919(209), 2891(58.05)（手動插入，is_synthetic=True）
- daily_price max: 2026-05-27
- daily_features: 205 symbols, max 2026-05-27
- bearish/bullish_features: 205 symbols

---

## 5. 已知問題

| 問題 | 狀態 |
|---|---|
| daily_quotes 16:00 時 rows 數待確認 | 明天觀察 |
| paper_broker.py `timestamp` column bug | backlog（#15 解決）|
| 進場訊號在 16:00 推送語意不正確 | backlog #15 |
| 手動部位 exit_scan_no_data | 預期行為（is_synthetic）|
| calendar_fallback_no_taiex_data warning | TAIEX 不在 daily_price |

---

## 6. v0.1.17 優先順序

```
P0: #15 decouple signal/submission (execution_submitter)
    T 16:00 daily_run → INTENT only
    T+1 08:30 execution_submitter → broker submission
    T+1 09:05 cancel sweep → EXPIRED

P0: #17 READY_FOR_SUBMISSION schema migration
    orders table: add status=READY_FOR_SUBMISSION
    positions table: 不動（等 v0.1.18）

P1: #16 open-gap calibration (INV-2)
    max_entry_gap_pct [ASSUMED] 0.03 → [CALIBRATED]
```

---

## 7. 下次對話帶回來

1. **這份 handoff**
2. `tail -30 logs/daily_run_cron.log`（確認 16:00 cron rows 數）
3. 如果 v0.1.17 開始：`docs/design/execution_submitter_design.md`
