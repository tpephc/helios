# Helios v0.1.16 Session Handoff — 2026-05-24

## Git HEAD
`cc33fa6` — feat: live_broker.py v0.1.1 + daily_run Step 7 auto-execute

## Session 工作內容

### 1. Shioaji 行情整合

#### `data/sources/shioaji_client.py` v0.1.0
- Read-only Shioaji client（stock snapshots + warrant liquidity）
- Context manager pattern: `with ShioajiClient() as client:`
- `stock_snapshots(stock_ids)` → Polars DataFrame with bid/ask/volume
- `warrant_call_contracts(underlying_names)` → TSE call warrant list
- `warrant_snapshots(contracts, max_spread_pct=5.0)` → liquidity metrics

#### `monitoring/quote_source.py` v0.1.16
- `ShioajiQuoteSource` 取代 `YFinanceQuoteSource` 為預設報價源
- `fetch_contract=False`：直接構造 minimal TSE Stock object，不下載 42k 合約
- 流量：~2MB/天（vs 舊版 21-61MB/天），降 ~50x
- `snapshot.ts`（nanoseconds）轉換為 UTC datetime，staleness 判斷正常
- OTC 上櫃股票暫不支援（defer）

#### `scripts/intraday_monitor.py`
- 預設報價源改為 `ShioajiQuoteSource`

### 2. Live Broker 整合

#### `execution/live_broker.py` v0.1.1
simulation=True  → StockOrderLot.Common（整股），lots=1
simulation=False → StockOrderLot.IntradayOdd（零股），shares=1，max 5
**設計決定：**
- 無 operator approval gate：自動下單 + Telegram 告知
- `submit_buy(lots=)` / `submit_buy(shares=)` 語意分離，傳錯 raise ValueError
- `execution_reason`: `placed` / `partial_filled` / `filled`
- `success=True` = 委託送出成功（非成交確認）
- VWAP fill price from deals list
- 整股 notional = `lots * 1000 * price`
- `broker_order_id` 寫入 `orders.broker` 欄位：`shioaji_sim:{id}`
- Live kill-switch：`live_trading_enabled=False`（.env 預設），`simulation=False` 時強制檢查

**Advisor review 採納 vs 暫緩：**

| 項目 | 決定 |
|---|---|
| VWAP fill price | ✅ 已修 |
| Partial fill 三態 | ✅ 已修 |
| 整股 notional ×1000 | ✅ 已修 |
| broker_order_id in DB | ✅ 已修 |
| Live kill-switch | ✅ 已修 |
| `time.sleep` → async | ❌ 暫緩（批次架構不適用）|
| Session pooling | ❌ 暫緩（每天一次 login 可接受）|
| NotificationDigest | ❌ 暫緩（premature abstraction）|
| SecureString | ❌ 暫緩（過度工程）|

#### `scripts/daily_run.py` Step 7
- 移除：`listen_for_approvals`、`PaperBroker`（Step 7）、`_resolve_short()`
- 新增：`LiveBroker.submit_buy(lots=1)` 對每個 pending signal 自動執行

### 3. DB 清理
- 198 筆 dev_bootstrap 合成持倉 → `status='CLOSED'`、`exit_reason='dev_bootstrap_cleanup'`
- OPEN positions: 0（乾淨起點）

### 4. Stop Logic Hardening
- `assert` → `RuntimeError`（invariant 不受 `python -O` 影響）
- `math.isfinite()` guard for NaN/Inf price
- Unknown PriceZone → `ValueError`（不再 silently fallthrough to NORMAL）

### 5. Shioaji API 探索結論

| 欄位 | 可取得 |
|---|---|
| 股票即時報價（bid/ask/close/volume）| ✅ snapshots() |
| 台股 Call 權證清單（TSE 27,550 筆）| ✅ category='00'+'購' |
| 履約價 / 到期日 / 行使比例 | ❌ 不在 contract schema |
| 下單（simulation）| ✅ 需 activate_ca() + signed=True |
| 下單（live）| ✅ 需 live_trading_enabled=True |

### 6. 流量狀況
- 今日已用：42.54 MB / 500 MB（8.5%）
- 零股成交金額不納入流量計算（整股才算）
- API 限制：login 1000 次/天、snapshots 50 次/5秒

---

## 設定檔

### `.env` 新增欄位
CA_CERT_PATH="/home/tradeagent/projects/helios/certs/Sinopac.pfx"
CA_PASSWORD="F121841329"
LIVE_TRADING_ENABLED=false
### `config/settings.py` 新增欄位
```python
ca_cert_path: str | None = None
ca_password: SecretStr | None = None
live_trading_enabled: bool = False
```

---

## Shioaji 測試報告（週一必做）

**時間：週一 08:00–20:00（非假日）**

```bash
cd ~/projects/helios
uv run python -c "
import shioaji as sj
from shioaji.constant import Action, StockPriceType, OrderType
from config.settings import get_settings
import time

cfg = get_settings()
api = sj.Shioaji(simulation=True)
api.login(
    api_key=cfg.shioaji_api_key.get_secret_value(),
    secret_key=cfg.shioaji_secret_key.get_secret_value(),
    fetch_contract=True, contracts_timeout=30000, subscribe_trade=True,
)
api.activate_ca(
    ca_path=cfg.ca_cert_path,
    ca_passwd=cfg.ca_password.get_secret_value(),
    person_id=api.stock_account.person_id,
)
contract = api.Contracts.Stocks.TSE['2890']
order = sj.order.StockOrder(
    action=Action.Buy, price=contract.reference,
    quantity=1, price_type=StockPriceType.LMT,
    order_type=OrderType.ROD, account=api.stock_account,
)
trade = api.place_order(contract, order)
print('trade:', trade)
time.sleep(2)
api.update_status(api.stock_account)
print('status:', trade.status)
api.logout()
"
```

跑完後到永豐金簽署中心確認「python 測試與否」→「已測試」。
審核通過後 `api.stock_account.signed` 變 `True`，`LiveBroker` 才能正常下單。

---

## 週一待辦

| 優先 | 項目 |
|---|---|
| P0 | 08:00 跑 simulation 測試報告 |
| P0 | 09:05 確認 intraday_monitor log 出現，`is_stale=False` |
| P0 | 16:00 daily_run：確認 LiveBroker 自動下單 → Telegram 通知 |
| P1 | `pyproject.toml` version bump to 0.1.16 |
| P1 | 確認 `signed=True` 後整合 daily_run simulation 完整測試 |

## Backlog

### v0.1.16
- `run_exit_scan.py` 改用 `LiveBroker.submit_sell()`
- `scripts/reconcile_fills.py`：日終對帳，確認委託成交狀態
- OTC 股票 `ShioajiQuoteSource` 支援

### v0.3.0
- 權證篩選（Shioaji 不提供履約價/到期/行使比例，待解）
- Shioaji live mode（`live_trading_enabled=True`）

---

## DB 狀態
| 表 | 狀態 |
|---|---|
| positions OPEN | 0（dev_bootstrap 已清除）|
| daily_features | 通過 2026-05-22 |
| company_metadata | 1088 筆 |
